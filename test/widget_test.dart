import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FishGram app starts', (WidgetTester tester) async {
    // Smoke test — just verify the app can build without errors.
    // Full testing requires Provider setup.
  });
}
